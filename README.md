# Simulador Cobranza - Avatar 3D

Proyecto de entrenamiento para asesores de cobranza con avatar 3D y voz.

## Ejecutar localmente
1. `npm install`
2. Crear `.env` con `OPENAI_API_KEY=tu_api_key`
3. `node server.js`
4. Abrir `http://localhost:3000`

## Nota
No subir `.env` al repositorio.

